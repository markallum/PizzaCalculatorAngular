import { Subject } from "rxjs/Subject";
// import { SetModel } from "./set-component/set.model";
import { Observable } from "rxjs/Observable";

export class CalculatorService {
    // sets: SetModel[] = [];

    
    // setResult = new Observable<number>();
    // calculated: Subject<SetModel[]>;
}